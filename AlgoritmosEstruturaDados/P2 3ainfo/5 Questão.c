#include <stdio.h>
struct ptr{
       char letra;
       struct ptr *prox;
       };
struct ptr *l1,*l2;
main(){
       char letra[100],l;
       printf("\t** JOGO DA FORCA **\n\n");
       printf("Voce, usuario, digite uma palavra que deve ser adivinhada. O (.) termina a execucao!\n");
       scanf("%c",&l);
       while(l!='.'){
                     letra[i]=l;
                     i++;
                     scanf("%c",&l);
       getch();
       
}
